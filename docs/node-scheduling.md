# 节点调度

节点调度是 Meridian 的可选分布式入口模块。主控保存节点、站点分配、流量周期和 DNS 状态；每台 VPS 只运行一个轻量 `meridian-agent`，负责 HTTPS 监听、站点反代、网卡流量采集和状态上报。

![节点调度总览示例](images/node-scheduling.png)

> 上图仅用于说明页面布局，节点地址、站点域名和统计数据均为虚构示例。

## 工作方式

```text
客户端 -> 站点域名 -> 精确 DNS A/AAAA -> 节点 Agent -> 站点上游 Emby/Jellyfin
                                      ^
                                      |
                               主控配置与状态
```

- 未启用调度的站点继续使用原面板入口，不会改变现有 DNS。
- 启用调度后，主控把站点 Host、上游线路、播放改写、TLS 证书和运行配置下发给目标 Agent。
- Agent 应用配置并通过 HTTPS/SNI 健康检查后，主控才创建或更新自己管理的精确 DNS 记录。
- DNS 切换只影响新连接，已经建立的播放连接不会被强制迁移。
- 节点流量额度仍按 Agent 采集的网卡总收发字节计算，包含该 VPS 上其他业务产生的流量。

## 前置条件

1. 主控必须通过有效的 HTTPS 证书访问，不能使用自签名证书。
2. 节点需要 Linux amd64 或 arm64、root 权限和可访问主控的出站 HTTPS。
3. 节点的 `端口` 必须没有被 Xray、Nginx、Caddy 或其他服务占用。443 被占用时，可以使用 9090、9443 等端口。
4. 站点域名需要由 DNS 服务商解析到节点地址。使用自动 DNS 时，在主控 TLS 设置中配置 Cloudflare DNS API Token。
5. 节点必须配置独立于面板证书的边缘证书和私钥。通过 `EDGE_TLS_CERT_FILE`、`EDGE_TLS_KEY_FILE` 指定；不能复用 `PANEL_TLS_CERT_FILE`、`PANEL_TLS_KEY_FILE`。证书 SAN 需要覆盖分配给节点的站点域名。Docker 主控可在已配置 Cloudflare ACME 凭据后执行 `docker exec meridian /app/meridian admin issue-edge-certificate` 自动签发隔离的 wildcard 证书。

## 创建并安装节点

1. 打开主控的“节点调度”页面，点击“添加节点”。
2. 填写节点名称、显示地址和 Agent HTTPS `端口`。
3. 设置流量上限、每月重置日、计费方式和优先级。流量上限填 `0` 表示不限额，重置日填 `0` 表示不自动重置。
4. 控制器地址填写当前主控的完整 HTTPS 地址，例如 `https://panel.example.com:9090`。
5. 保存后，面板会显示一次性注册令牌和一键安装命令。只在目标节点上执行该命令。

典型命令如下，实际令牌必须使用面板生成的值：

```bash
wget -qO- https://panel.example.com:9090/api/agent/install.sh | sudo bash -s -- \
  -c https://panel.example.com:9090 \
  -t ONE_TIME_ENROLLMENT_TOKEN
```

安装器会从同一个主控下载并校验匹配版本的 Agent 二进制，创建并启动 `meridian-agent.service`。独立二进制安装的主控会将 Agent 安装到 `/usr/local/bin/meridian-agent`，升级时与主控成对更新，失败时成对回滚。注册成功后，一次性令牌会从节点删除并换成长期 Agent 凭据；不要把令牌写入公开脚本或提交到 Git 仓库。

## 验证 Agent

在节点上执行：

```bash
sudo systemctl status meridian-agent --no-pager
sudo journalctl -u meridian-agent -n 100 --no-pager
sudo ss -lntp | grep ':9090'
```

在主控的节点调度页面确认：状态为“在线”、网卡名称已识别、配置状态为“配置已应用”。如果节点端口不是 9090，把最后一条命令中的端口替换为实际值。

## 配置站点调度

站点配置位于同一个“节点调度”分组：

1. 在“站点调度”找到目标站点。
2. 勾选“启用节点调度”。
3. 选择“跟随全局调度”或“固定节点”。固定模式还要选择具体节点。
4. 点击“保存”，等待 Agent 应用配置和 DNS 健康检查完成。

站点状态会显示期望节点、生效节点、节点端口、DNS 状态、最近请求和最终 HTTP 状态。停用调度时，Meridian 只删除自己创建并记录的精确 DNS 记录，不会删除用户自行创建的同名记录。

## 自动调度规则

自动模式的实际判定顺序如下：

1. 排除未启用的节点。
2. 排除心跳超时、状态不是“在线”的节点。
3. 排除已达到流量额度的节点。额度和周期按节点网卡总流量计算。
4. 按优先级从高到低排序，优先级数值越大越优先。
5. 优先级相同时，选择当前周期已用流量较少的节点。
6. 没有合格节点时，站点保持等待状态，不会把 DNS 切到不健康节点。

手动模式只使用管理员选中的节点，但该节点仍必须在线、启用且未达到额度。固定站点节点同样需要通过这些可用性检查。

## 升级与重装

正常更新主控后，Agent 会通过已认证的配置接口获取新二进制，校验 SHA-256 后原子替换并重启。普通主控升级不会使节点令牌失效，也不需要重复安装。

只有以下情况才需要重新生成脚本并重新注册：

- 管理员在面板中主动刷新节点注册令牌；
- 节点上的 Agent 状态文件被删除；
- 节点更换到另一套主控；
- 节点长期离线且日志显示令牌已被撤销。

## 删除节点

先在面板删除节点，主控会撤销该节点的 Agent 授权。然后在节点执行卸载步骤。删除节点不会自动删除节点上其他业务进程，也不会修改 Nginx、Caddy 或 Xray 配置。
